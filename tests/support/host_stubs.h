#ifndef HOST_STUBS_H
#define HOST_STUBS_H

/* Minimal 3DS types for compiling musiclist.h on the host test runner. */
#include <stdint.h>

typedef uint32_t u32;

typedef struct PrintConsole PrintConsole;

#endif /* HOST_STUBS_H */
